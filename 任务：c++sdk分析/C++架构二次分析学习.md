
手的sdk的力矩应该是力矩限幅，力矩限幅不是控制力矩，是限制力矩在一个范围内，包括速度指令码，都是限幅。
## 整体的一个数据流向或者架构

在整体的一个设计的时候，避免假想一些并发或者线程安全问题，这些问题可能通过架构就会得到解决，**对于用户来说，可能永远不会接触到一些接口，流程可能就已经固定了。**  一定要区分开发者和用户两者的关系。


开启canmanager ---> 订阅功能模块的回调函数（一直运行） ---> 回调函数处理信息逻辑 ---> 回调函数中通知waiter进行获取can发送过来的信息
### 从anglemanager进行分析：各个模块耦合进来
#### 一：职责分离，将功能独立出来，使用**组合模式**

在一个类中，让其负责的任务单一化，将对应的东西进行拆分实现。比如说手的速度控制，角度控制。添加回调函数。**通过can的订阅机制，由各自去处理信息。**

只要canmanager订阅了功能模块中的回调函数就会一直进行处理。收发是一直进行的，
- 在功能模块中要考虑多线程处理的问题。
	- 在这里面就使用了一个**waiters的对象进行管理订阅信息**，通过有信息就通知的模式。这样做的好处是避免cpu轮询和忙等去查询**信息是否过来。**
	- 使用锁需要轮询的意思就是锁只会阻塞获没获取到锁，其不会检查有没有数据，需要一直轮询是否存在数据并且发送。
>[!waiter+条件变量 ]
>waiter的作用就是进行不同线程中的**异步到同步的一个必要条件（主要就是跨线程同步）。条件变量等待。**----**精准唤醒（可能多个线程同时需要），零cpu等待，支持超时。**。 主要就是同步，等待数据的返回。
>与全局变量等设计相比，全局变量会使用到锁，多个线程可能会争夺资源。并且可能会轮询抢占cpu的资源。还会存在新旧数据的问题。
>
>**二次学习waiter**：
>1. 在使用waiter的场景下，其是作为线程间数据同步的一个作用，在当前的使用当中，主要是**阻塞获取关节数据（get_angle_blocking）** 的时候会用到,他的作用就是等待返回的数据，如果有多个线程同时想要获取这个数据，就会产生相对应的一个waiter，然后再数据到达时唤醒这些waiter进行**同步数据**。
>2. 如果只有一个线程需要这个阻塞数据的话，其实使用**全局变量和锁的方法**，同样也是可以实现的。（**锁也可以进行阻塞）** 
>	1. 但是全局变量和条件变量是可以实现同样的效果的，针对单线程。**条件变量是可以阻塞的（cv.wait），条件变量必须配合锁进行使用。** 

>[!锁和条件变量的学习]
>`m.lock()` 等的是“锁可用”（排队进门，互斥），`cv.wait(lk)`（等东西到货） 等的是“条件成立”
>1. 锁（mutex）：
>	1. 锁也有很多锁，
>		1. 普通的锁就是阻塞等待，其阻塞等待的是锁，其他线程持有锁其会阻塞，**但是他不会等待数据的到来，面对的只有锁** 。
>		2. 也有不阻塞等待的锁（其实是锁的一个函数），拿到锁返回true，拿不到就返回false。
>		3. 还有一类锁就是**自旋锁**，这类锁不是标准库中的，这类锁就会一直轮询占用cpu。
>	2. 锁的应用场景主要是规定能不能进入互斥区，**没有什么条件，只是争夺锁进入互斥区** ，其不会检测有没有达到对应的条件。
>	3. 他不会检测数据是否被消耗，拿到锁就会进入到下一流程。如果是**需要消费数据的场景，就会带来死锁的问题。** 
>2. 条件变脸cv：
>	1. 条件变量必须与锁配合使用，其会检测是否达到出发的条件，比如说数据是否到达，温度是否合格等。
>	2. 他被唤醒之后同时也会去争夺锁，同时也会检测数据是否已经被消耗。
>
>总结：两者面对的场景是不同的，如果只是需要保护互斥区，只需要锁就可以实现，如果需要条件判断。典型的生产消费场景则是条件变量。




- 一定要处理好边界问题，对于对象的析构或者close等逻辑都要考虑到多线程同时进行操作的问题。在这里面就是**关闭流式访问的上锁问题。确保避免在stop stream的时候不会访问。** 
#### 二：将通信层独立出来，统一由canmanage进行管理收发。

通过**订阅机制**进行管理每一个流经can的信息，由统一的can对象进行处理每功能模块。统一让回调函数去各自的功能模块去处理信息。**canmanager只负责收发信息。**

- 在这个逻辑中需要处理的东西就是**取消订阅要进行检查**，采用的是快照模式进行处理订阅的功能回调。----这个就是UAF问题。。。。**use-after-free**。
	- 通过增加一个**订阅状态结构体**来进行判断是否在其他线程中已经取消了订阅，保证状态的一致性。--在每次处理订阅的时候都进行检查。。。
	- 还有就是socket通信的关闭状态，对于这样的多线程共享变量（状态不一致导致的行为不同），一定要增加**状态检查机制**，常见的可能就是一个线程要发送，但是另一个线程已经进行了关闭，导致了文件描述符为-1.

#### 三：一定要考虑关闭函数或者析构函数的逻辑，避免多线程同时执行。

对于手的close函数，使用原子操作进行约束，确保线程安全。
```
if (closed_.exchange(true, std::memory_order_acq_rel)) {
return;
}
```
上述的这个原子操作函数会将**多个同时执行的线程按照内存序进行*排序*执行**，一个线程执行之后，其他线程会读取这个线程执行之后的状态。不会出现多个函数同时执行。


#### 四：如果存在回调的话，考虑回调在哪个线程中执行，考没考虑阻塞的问题。
在当前程序中，canmanager订阅各功能模块。订阅后再消息到达后，会使用线程去处理这些回调函数，在之前的方案中是将其顺序处理回调，可能会引起**阻塞问题**，这样在实时性上可能就会出现问题。

这个也就对应到了ros中的一个执行器，SingleThreadedExecutor这个执行器就是单线程的执行器，会阻塞，MultiThreadedExecutor是一个多线程执行器，会有一个线程池，在线程池里面进行处理对应的回调函数。

>[!二次修改总结]
>考虑清楚是否需要设计线程池，线程池会不会带来额外的开销（并发问题或者资源开销），一定要考虑清楚线程池是在处理什么，**平衡性能和资源的占用。在这个例子中**
>1. 串性处理回调，回调只是一个收发的任务处理，耗时很短，在微秒级就可以处理完成。
>2. 如果换成多线程的话，会带来额外的开销和资源占用。

>[!二次学习知识]
>什么是回调，回调函数的作用就是将两个线程中的数据通信，连接两个线程，让数据进行流动。无延迟等问题将数据流进行传递。**处理回调的线程或者对象通常是一个主要处理单一任务持续进行的一个对象任务。** 将这个对象放入到另一层中，回调函数处理的逻辑在具体的任务中（另外一个线程对象）
>1. 比如说在手的sdk中的作用就是：canmanager通过回调将接收过来的指令信息（其只是收发）发送给对应的线程回调函数，让回调函数进行处理具体的逻辑（就是让功能manager进行处理）。**（这样就实现了一个数据的流通）**
>2. 在手部遥操的代码中，回调函数循环是在提供数据源中的线程中，其主要的任务就是将手套的发来的数据不断提供给pipeline管线，pipeline管线的具体回调函数进行处理数据，通过在pipeline注册发送到机械手的回调将数据在发送给映射器。
>	1. 在这里pipeline充当一个调度器，将数据源发来的信息将给映射器进行处理，然后再将处理后的数据通过回调进行发送到指定的机械手或者其他逻辑，回调处理什么都是可以的，比如说ros命令，比如说真实机械手的命令。




#### 五：考虑生命周期检查机制

在当前项目中的一个特点就是所有的功能组件（模块）都是**公共暴露出来**，当执行close后，再次调用这个功能成员可能会产生未定义的行为。**功能模块manager里面没有生命周期检查机制**。

一个线程执行close，生命周期结束，可能另一个线程还可以执行angle.set等。

#### 六：当数据较多时，需要聚合时，也是需要进行分层聚合

在这里面就是传感器数据的一个聚合，首先将单个手指作为一个类聚合单个手指的帧，然后在使用一个上层传感器类聚合这些手指，聚合五个手指。
1. 在这里面，创建一个结构体，利用结构体将聚合进行实现。聚合单个手指的数据帧，每个手指都需要12个帧来组合数据。**这样是实现职责单一，各自负责各自的事情。**
2. 总传感器类只需要将各自单个手指进行聚合，然后调用其中的接口就可以。

---


首先将考虑一些多线程所遇到的问题，主要包括：

1. 析构函数所带来的一些**竞态问题**：
   (a) 对象在生命周期内的状态管理
   (b) 生命周期所涉及的契约关系
2. 幂等性（Idempotency）问题：
   (a) 确保多次执行函数会有一致的效果
3. 主要就是考虑在多线程中的一些**数据操作问题，一个线程中的状态导致另一个线程中行为。** 
4. 什么是原子性，原子变量，原子操作。原子操作**不是不可抢断的**，也可能会发生线程切换等操作。
	1. **原子性保证的是值的一致性**，要么是**旧值**要么是**新值**。只有这两种状态。原子和互斥是两种东西。
	2. 保证对某一个共享变量的**一次操作**不可分割。
	3. 与锁的区别是，锁可能对多个变量进行操作（框住一段代码），而原子操作是对一个变量进行操作。
---
#### 多线程下的一些原理

现在的计算机都是多核结构的，完全可以处理多线程的一个任务，为什么还是会需要锁和竞态分析等一些措施。
1. 主要就是在计算机中，多核处理的是**逻辑算法，但是他们却是共享的同一个内存**。

在内存中会出现什么问题，其中一个线程读取了一个对象的状态，进行存储。然后另一个线程在处理逻辑的时候又改变了对象的状态。但是第一个线程已经读取了。故出现了**同时读写同一块内存导致的数据不一致。** 会出现大量的问题。
	在c++中就有这样的一个问题，比如说vector对象，一个线程中已经读取了其的容量，另一个线程却添加或者删除一个对象，**导致容量变更，第一个线程在使用vector对象时，最终导致迭代器崩溃**。。。。


---



**1. 在考虑关键架构设计的时候，主要考虑的东西就是对象销毁所带来的一些东西**：
对象销毁与资源释放的关键函数处理：

主要考虑的问题是在析构（Destructor）或者 close、stop 等一些关键函数下，关于多线程以及静态资源处理的问题。

这些问题主要表现在：
1. 关闭操作是否会带来竞态或死锁
2. 静态资源的释放顺序
3. 确保关闭逻辑的安全性与稳定性

---


### 在c++二次分析中所遇到的问题
1. 在 L6 的 close 函数中，close 变量属于布尔（bool）量。**没有锁或者原子操作**，而是在最后将bool变成true。

在多线程环境下，如果只是简单的布尔量，可能存在数据竞争（Data Race）的问题，从而导致多次执行 close 逻辑。两个线程前后执行close函数，导致多次执行里面的逻辑。

2. Use-After-Free 风险
这个风险是某个对象在使用后在某个线程中进行了释放。在当前的 C++ 分析中，这类问题出现在订阅之后：系统会将任务推入一个订阅队列，并在一个循环线程中处理这个回调函数。
如果我将这个回调取消订阅，回调线程中的拷贝对象可能仍在执行相关的回调逻辑，因此会出现 Use-After-Free（UAF）风险。

拷贝复制对象无法保证回调已经结束。---**（这个属于是快照分发方法，可以最大限度的处理并行任务。避免竞态。）**


3. 状态检查缺失
这个状态检查其实也出现在多线程调用的情况中。在多线程调用中，一个线程获得并需要使用一个对象，而在另一个线程中，我将这个对象关闭并改变了它的状态。

如果在前一个线程中没有检查该对象的状态就直接进行操作，会导致未定义的行为（Undefined Behavior）。

在这版 C++ 的处理当中，针对 Socket 的文件描述符（file descriptor），我在一个线程中处理该描述符并进行 write 发送，而另一个线程却将该文件描述符关闭（stop），导致其返回值为 -1。

在这种情况下，如果需要调用 write 函数的线程没有提前进行状态检查，就可能会产生未定义的行为。

---
