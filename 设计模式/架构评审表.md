
  > 评审目标：用最少的检查项覆盖最常见、最高风险的“生命周期/并发/回调/资源释放/工程化交付”问题。
  > 使用方式：每一项给出 结论（通过/不通过/有风险）+ 证据（文件/符号/测试/日志）。

  ## 0. 基本信息（必填）

  - 项目名称/版本/提交：__________
  - 运行环境与依赖（OS/内核/运行时/库）：__________
  - 并发模型概述（线程/协程/事件循环）：__________
  - 外部资源（网络/FD/设备/数据库）清单：__________
  - 关键 SLA/非功能指标（延迟/吞吐/可靠性/可用性）：__________

  ———

  ## 1. 生命周期与状态契约（Lifecycle Contract）

  ### 1.1 状态机定义（必须有）

  - [ ] 是否明确定义状态机（如 Created/Running/Closing/Closed）？
    证据：__________
  - [ ] 每个公开 API 是否标注“允许在哪些状态调用”？
    证据：__________

  ### 1.2 close/stop/析构关系（必须统一）

  - [ ] close()/stop() 与析构的关系是否明确（析构是否调用 close/stop，是否 best-effort，是否吞异常）？
    证据：__________
  - [ ] close()/stop() 是否幂等（多次调用只生效一次）？是否允许并发调用？
    证据：__________
  - [ ] 关闭顺序是否定义且合理（先停后台任务/流，再停底层 IO/资源）？
    证据：__________

  ### 1.3 close 后统一行为（必须一致）

  - [ ] close 后调用行为是否统一（统一抛错 or 统一返回约定值/空值），并在文档中明确？
    证据：__________
  - [ ] 是否存在“Facade/入口类绕不过去的状态检查”？若内部子组件被直接暴露，是否明确告知用户风险与行为？
    证据：__________

  > 建议产物：一张 “API × 状态” 行为矩阵（close 后每个 API 抛什么/返回什么）。

  ———

  ## 2. 并发与数据竞态（Thread Safety）

  ### 2.1 共享状态与锁策略

  - [ ] 共享状态是否全部满足：原子 或 同一把锁保护 或 线程局部？
    证据：__________
  - [ ] 是否存在 “检查后使用（TOCTOU）” 竞态窗口（例如 stop 与 send 同时发生）？是否有二次检查/锁内检查兜底？
    证据：__________
  - [ ] 锁顺序是否固定（避免 ABBA 死锁）？是否有跨模块锁嵌套风险？
    证据：__________

  ### 2.2 stop 与并发调用的交互（必须定义）

  - [ ] stop/close 与业务 API 并发时，允许的结果集合是否明确（成功 or 抛 StateError；是否“至少一次/至多一次”语义）？
    证据：__________
  - [ ] stop 返回后是否保证：后台线程已退出、回调不再执行、资源已释放（或明确说明不保证）？
    证据：__________

  ———

  ## 3. 回调/事件语义（Callback Semantics）

  ### 3.1 回调执行上下文（必须写清楚）

  - [ ] 回调在哪个线程/事件循环执行？是否串行？是否可能并发重入？
    证据：__________
  - [ ] 是否允许回调阻塞？若不建议/禁止，是否在文档明确“红线规则”？
    证据：__________
  - [ ] 回调异常如何处理（吞掉/日志/传递给上层/触发熔断）？是否一致？
    证据：__________

  ### 3.2 subscribe/unsubscribe 并发策略（高风险必查）

  - [ ] subscribe/unsubscribe 是否线程安全？是否允许在回调栈内调用？
    证据：__________
  - [ ] unsubscribe() 返回时是否保证：该回调不再被调用？若不能保证，是否有 in-flight 同步机制（引用计数/栅栏/等待）？
    证据：__________
  - [ ] 是否明确禁止“在回调线程中 teardown（析构/stop）”这类危险操作？是否有防御（断言/检测线程/避免自 join）？
    证据：__________

  ———

  ## 4. 阻塞/超时/取消（Blocking / Timeout / Cancellation）

  - [ ] 所有阻塞 API 是否都有明确超时参数与默认值？timeout<=0 等边界是否校验？
    证据：__________
  - [ ] stop/close 是否会唤醒阻塞等待（可取消语义）？若不会，是否在文档说明，并给出建议（缩短 timeout/外部 cancel）？
    证据：__________
  - [ ] 阻塞等待涉及注册/排队时，失败路径是否回滚（避免 waiter 泄漏/状态残留）？
    证据：__________

  ———

  ## 5. 流式/队列/背压（Streaming / Queue / Backpressure）

  - [ ] stream() 返回值是否“稳定可用”（避免返回内部引用在并发 stop 后失效）？
    证据：__________
  - [ ] 流式线程异常退出时，消费者是否能被唤醒并结束（例如关闭队列/发送终止信号）？
    证据：__________
  - [ ] 背压策略是否定义（队列满：丢旧/丢新/阻塞/错误）并一致实现？是否可配置？
    证据：__________
  - [ ] “关闭队列”与“停止采集”的语义是否区分清楚（避免用户以为丢弃队列=停止后台线程）？
    证据：__________

  ———

  ## 6. 异常安全与错误模型（Exception / Error Taxonomy）

  - [ ] 析构函数是否保证不抛异常（避免 terminate）？close/stop 是否 best-effort 并有可观测性？
    证据：__________
  - [ ] 错误分类是否清晰（Validation/State/IO/Timeout/Cancelled），并在文档列出？
    证据：__________
  - [ ] 同类错误是否抛同类异常/错误码，错误信息是否稳定可匹配（便于用户处理）？
    证据：__________

  ———

  ## 7. 资源管理（FD/线程/内存/外设）

  - [ ] 资源所有权是否单一清晰（RAII/引用计数/显式释放）？
    证据：__________
  - [ ] stop/close 是否 join 所有线程，避免悬挂线程或进程退出卡死？
    证据：__________
  - [ ] 是否存在自 join 风险（在自身线程里 join 自己）？是否规避？
    证据：__________

  ———

  ## 8. 文档与可观测性（Doc / Observability）

  - [ ] 文档是否区分“示意/伪码/真实实现”，避免文档-实现漂移？
    证据：__________
  - [ ] 是否把“红线规则”显式写出（回调线程禁止 teardown、析构前必须无 in-flight 调用等）？
    证据：__________
  - [ ] 是否提供可观测性（日志/事件/统计/错误码），并能定位并发/关闭相关问题？
    证据：__________

  ———

  ## 9. 工程化交付（Build / Test / CI / Release）

  - [ ] 是否有最小上手文档（Quickstart）与可运行示例（examples）？
    证据：__________
  - [ ] 是否有自动化测试覆盖关键边界（并发 close/stop、unsubscribe 与回调 in-flight、阻塞超时等）？
    证据：__________
  - [ ] 是否有 CI（至少 build + unit tests），并建议开启 sanitizer/静态分析（TSAN/ASAN/UBSAN 或同类工具）？
    证据：__________
  - [ ] 是否具备发布信息（版本策略/变更记录/许可证/安装方式）？
    证据：__________

  ———

  ## 10. 评审结论（建议格式）

  - 风险级别：低 / 中 / 高
  - 上线/上架建议：允许 / 有条件允许 / 不建议
  - 必须整改项（Blocking）：1)… 2)…
  - 建议整改项（Nice-to-have）：1)… 2)…

  ———