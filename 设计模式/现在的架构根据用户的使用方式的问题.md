1. c++在启动的时候容易忽略左右手的问题，导致阻塞。---**这个是主要是**
	在读取的时候会出现一个问题，比如说当手是错误的时候，带来的问题就是一直阻塞，队列一直等待数据，无法进行关闭，stop不了。
2. 在读取压力传感器的数值的时候对于不了解的用户可能带来困惑--**这个可能使用的方式不同，可能有人需要这样的一个数据流导出等使用。**，可以提供一个打印的函数。
	1. 其是获取8✖️9的一个矩阵数列，如果读取不正确的话会显示不出来，比如我就是直接输出，数据长度太长而导致的省略。。。。**输出也必须是一个举证的形式。**。 