### ros中spinonce与spin的区别
1. spinonce就是每次只处理一次队列里面的数据，到函数这里就会处理队列里面全部的数据。他不会阻塞，通常需要配合while（ros：：ok）进行使用。spinonce可以处理一些其他的逻辑，**他在运行一次就会再次进入循环处理逻辑。** 
2. spin函数是一个阻塞函数，里面应该是存在while循环，所以不需要配合while进行。spin会一直**阻塞在处理队列那里，不会执行其他函数。** 所以如果需要处理其他的逻辑就需要使用spinonce。